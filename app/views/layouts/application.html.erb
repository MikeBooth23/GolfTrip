<!DOCTYPE html>
<html>
  <head>
    <title>Base Rails</title>
    <%= csrf_meta_tags %>

    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.css">
  <style>
  body {
    background-image: url("https://wallpaperset.com/w/full/1/a/8/496123.jpg");
    background-size: cover
  }
  </style>

  </head>

  <body>
<nav class="navbar navbar-expand-lg bg-body-tertiary">
<div class="container-fluid">
<div class="collapse navbar-collapse" id="navbarNav">
  <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" href = "/"> Home </a>
      </li>
      <% if session.fetch("user_id") == nil %>
      <li class="nav-item">
        <a class="nav-link" href = "/user_sign_up"> Sign up </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/user_sign_in">Sign in</a>
      </li>
      <% else %>
      <li class="nav-item">
        <a class="nav-link" href="/user_sign_out">Sign out</a>
      </li>
      <% end %>
      <li class="nav-item">
        <a class="nav-link" href = "/members"> Members </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href = "/historicals"> Historical Results </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href = "/upcomings"> Upcoming Trip </a>
      </li>   
      <li class="nav-item">
        <a class="nav-link" href = "/galleries"> Gallery </a>
      </li>                     
    </ul>
  </div>
  </div>
  </nav>


  <% if notice.present?%>
  <div style="color: green">
  <%= notice%>
  </div>
  <% end %>

  <% if alert.present?%>
  <div style="color: red">
  <%= alert%>
  </div>
  <% end %>
    <%= yield %>
  </body>
</html>
